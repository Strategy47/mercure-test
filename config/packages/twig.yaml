twig:
    default_path: '%kernel.project_dir%/templates'
    debug: '%kernel.debug%'
    strict_variables: '%kernel.debug%'

    globals:
        mercure_url: '%env(MERCURE_PUBLISH_URL)%'
        mercure_jwt: '%env(MERCURE_JWT_SECRET)%'
